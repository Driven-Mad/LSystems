#include "lParser.h"

bool lParser::writeObj(char * filepath, object lObj)
{
    //Object writer (ref old code - re-write for c++ 11
    bool success = false;
    return success;
}

bool lParser::loadObj(char * filepath, object &obj)
{
    //Object loader (ref old code - re-write for c++ 11
    bool success = false;
    return success;
}

bool lParser::loadLObj(char * filepath, lObject lObj)
{
    bool success = false;
    return success;

}


bool lParser::writeLObj(char * filepath, lObject lObj)
{
    bool success = false;
    return success;
}
